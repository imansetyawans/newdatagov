import { LoadingState } from "@/components/ui/LoadingState";

export default function Loading() {
  return <LoadingState label="Loading page" description="Fetching the latest governance workspace." />;
}
